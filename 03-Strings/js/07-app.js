const producto = "Monitor de 20 Pulgadas";

console.log(producto.toUpperCase()); //trasformar todo a minuscula
console.log(producto.toLowerCase()); //trasformar a miniscula

const valor = 200;
console.log(typeof(valor));
console.log(valor.toString());//cnvertir de numero a string